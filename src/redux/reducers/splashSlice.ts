import { createSlice } from "@reduxjs/toolkit";

export const splashSlice = createSlice({
    name : "splashSlice",
    initialState:{text:"Arun------"},
    reducers:{

    }
})

export default splashSlice.reducer

